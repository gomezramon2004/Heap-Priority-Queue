#include "./queue/queue.hpp"
#include <iostream>
#include <string>

int main() {
    try {
        
    } catch (const std::runtime_error &e) {
        std::cerr << e.what() << std::endl;
    }
    return 0;
}